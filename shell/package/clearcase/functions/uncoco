# ZSH function file
# Part of the clearcase package
# Un-checkout checked out files
# Usage: uncoco [-r]
#	 -r	Recurse
#
# $Id: uncoco,v 1.2 2001/03/14 09:55:01 mebourne Exp $

local recurse="$1"
local files

files=($(cleartool lscheckout -me -cview -short $recurse))
echo "REVERT all of these files?"
echo - ${(F)files}
if read -q
then
  cleartool unco -rm $files
fi
