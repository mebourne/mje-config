# ZSH function file
# Part of the SQL Oracle package
# Fetch and return the list of columns for the current table
# For use only by the SQL packages
#
# $Id: _oracle_fetchcolumns,v 1.3 2002/05/24 16:13:54 mebourne Exp $

local table="$argv[1]"

{
  echo "set pages 0 feed off"
  echo "select column_name from ${_sql_parameters[access]:-user}_tab_columns"
  echo "where table_name like '$table';"
} | _oracle_rawsql
