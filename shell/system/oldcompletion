# ZSH configuration file
# Executed for interactive shells via zshrc
# Old style completion setup. To be phased out eventually
# Written by Martin Ebourne
#
# $Id: oldcompletion,v 1.1 2001/02/12 18:13:24 mebourne Exp $

compctl -c type whence where
compctl -g '*(-/) .*(-/)' pushd mkdir dircmp
compctl -g '*(/) .*(/)' rmdir
compctl -A shift
compctl -F unfunction
compctl -a unalias
compctl -l '' nohup noglob exec nice eval - time rusage
compctl -v unset typeset declare vared readonly export integer
compctl -j -P "%" fg disown
compctl -z -P "%" bg
compctl -l '' -x 'p[1]' -f -- .
compctl -k hosts ftp host rup rusers
compctl -k hosts -x 'p[2,-1]' -l '' -- rsh
compctl -f -x 'p[1], p[2] C[-1,-*]' -k groups -- chgrp
if [[ -r /proc/filesystems ]]
then
    # Linux
    filesystems='"${${(f)$(</proc/filesystems)}#*       }"'
else
    filesystems='ufs 4.2 4.3 nfs tmp mfs S51K S52K'
fi
compctl -g '*.Z' + -g '*(-/)' uncompress zmore
compctl -g 'RCS/*(:s@RCS/@@:s/,v//)' co rlog rcsdiff
