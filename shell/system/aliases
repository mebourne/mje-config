# ZSH configuration file
# Executed for interative shells via zshrc
# Command alias setup
# Written by Martin Ebourne
#
# $Id: aliases,v 1.2 2001/03/01 15:27:16 mebourne Exp $

alias rm="rm -i"
alias which-command=which
alias mv="nocorrect mv"
alias cp="nocorrect cp"
alias mkdir="nocorrect mkdir"
alias jobs="jobs -l"

# Only alias more to less if less is present on the system
which less | grep / >/dev/null && alias more=less

# If we've got the GNU ls then enable automatic colour, else don't
if [[ $(ls --version 2>&1) == *GNU* ]]
then
  alias ls="ls -F --color=auto"
else
  alias ls="ls -F"
fi
alias dir="ls -al"
