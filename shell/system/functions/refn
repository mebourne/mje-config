# ZSH function file
# Register new autoload function. Re-registers if already defined
# Usage: refn <function-name>
#
# $Id: refn,v 1.4 2001/02/21 12:08:51 mebourne Exp $

local fns
fns=($*)

if (( $#fns ))
then
  unfunction - $fns 2>/dev/null
  autoload -U - $fns
else
  echo "Syntax: refn <function-name> ..." 1>&2
  return 1
fi
